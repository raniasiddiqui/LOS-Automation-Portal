# The portal reuses the parent project's crawler, so install ../requirements.txt too.
playwright>=1.45.0
streamlit>=1.30.0
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
PyYAML>=6.0            # fixtures (Phase 2)

# After installing:  playwright install chromium
